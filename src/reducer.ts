import { State } from "./state";
import { DomEvent } from "./event";
import { match, __, not, select, when } from "ts-pattern";

// const reducer = (state: State, event: DomEvent): State => {
//   match<[State, DomEvent], State>([state, event]).with();
// };
